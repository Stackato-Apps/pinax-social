applications:
- name: pinax-social
  docker_image: stackatoapps/pinax-social:latest
  memory: 128M
  services:
    ${name}-db:
      type: mysql
  stackato:
    min_version:
      client: 1.4.3
  timeout: 180
